# Dario proxy auth key — every client must send this as ANTHROPIC_API_KEY.
# Generate with: openssl rand -hex 32
DARIO_API_KEY=CHANGEME_openssl_rand_hex_32
# Bind on all interfaces inside the container so other containers on dario.network can reach it.
DARIO_HOST=0.0.0.0
DARIO_PORT=3456
