global:
  scrape_interval:     30s
  evaluation_interval: 30s
  external_labels:
    instance: raspberrypi

# Alloy ships all metrics via remote_write — Prometheus only
# needs to scrape itself for self-health dashboards.
scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']
